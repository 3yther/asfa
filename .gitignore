# Secrets. This repo is its own git root, so the parent repo's .gitignore does
# NOT apply here — these rules have to be stated locally.
.env
.env.*
!.env.example
*.pem
*.key
secrets/
*.db
*.sqlite
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.env.local
*.log
.DS_Store
token.json
credentials.json
google_token.json
repomix-output.xml
.obsidian/
cookies.txt
test_leak.txt
static/uploads/fragrances/
static/uploads/gym-photos/
# FragDB reference dataset (Tier 3 Part 6) — downloaded locally, never committed
scripts/data/
*.db-wal
*.db-shm
# CV PDF served by /cv — personal document, this repo is public
static/cv/
