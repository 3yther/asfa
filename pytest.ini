[pytest]
# Without `norecursedirs`, a bare `pytest` aborts during collection on
# odysseus/tests/conftest.py (a separate vendored project with its own
# dependencies) and runs nothing at all.
testpaths = tests test_api_keys.py
norecursedirs = odysseus venv .venv env node_modules .git __pycache__
